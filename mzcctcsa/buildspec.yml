version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto11
  build:
    commands:
      - echo Build Starting on `date`
      - chmod +x ./gradlew
      - ./gradlew build
      
artifacts:
  files:
    - /build/libs/mzcctcsa.jar
#  base-directory: 'target/my-web-app'
